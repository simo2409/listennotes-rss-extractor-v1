[project]
name = "listennotes-rss-extractor-v1"
version = "0.1.0"
description = "Download and transcribe new podcast episodes from an RSS feed"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "feedparser>=6.0",
    "requests>=2.32",
]

[project.scripts]
extract = "main:main"
